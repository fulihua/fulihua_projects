spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/bookmanager_db?useSSL=false&serverTimezone=UTC&characterEncoding=utf8
    username: root
    password: abcd1234


mybatis:
    mapper-locations: classpath:mapper/*.xml
    type-aliases-package: org.example.library.entity

logging:
  level:
    org.springframework.web: DEBUG
    org.example.library: DEBUG
    com.zaxxer.hikari: DEBUG